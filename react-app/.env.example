# Supabase Configuration
# These are PUBLIC keys safe for client-side use
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# The ANON key is designed to be public and is safe to expose
# It only works with your Row Level Security (RLS) policies
# Never put SERVICE_ROLE keys in client-side code!